# Mock Data Generator

**Генератор моковых данных на Java** с поддержкой различных типов полей и вывода в CSV/JSON.

- Чтение конфигурации из app.properties
- Поддержка полей: int(min,max), name, email;
- Вывод в форматах: **CSV** и **JSON**;
- Покрытие тестами (JUnit 5 + Mockito);
- Полная документация кода через **JavaDoc**;
- Сборка **fat JAR** через Shadow Plugin;
- CI/CD на **GitHub Actions** (JDK 11 / 17 / 21, кэширование, публикация артефактов)

## Технологии

- Java 17 (совместимо с 17, 21)
- Gradle 8.14 + Wrapper
- JUnit 5, Mockito
- Shadow JAR
- GitHub Actions

## Запуск

### Вариант 1: Через готовый JAR (рекомендуется)

1. Перейди в [Actions](https://github.com/Daniilsol200/mock-data-generator/actions)
2. Выбери последний успешный запуск
3. Скачай артефакт → mock-data-generator-jdk17-...jar или jdk21
4. Запусти:

### Вариант 2: Локальная сборка

bash
java -jar mock-data-generator-jdk17-xxxxxx.jar
Вариант 2: Локальная сборка
Bash./gradlew shadowJar
java -jar build/libs/mock-data-generator-1.0.0.jar

1. Перейди в [Actions](https://github.com/Daniilsol200/mock-data-generator/actions)
2. Выбери последний успешный запуск
3. Скачай артефакт → mock-data-generator-jdk17-...jar или jdk21
4. Запусти:

Конфигурация (src/main/resources/app.properties)

generator.rows.count=100
output.format=CSV
generator.fields=id:int(1,100000),user_name:name,user_email:email

Вывод в консоль:
csvid,fullName,email
4582,Иван Петров,ivan.petrov@example.com;
7219,Мария Сидорова,m.sidorova@example.com;
...

Как выполнить сборку проекта
Bash# 1. Клонировать репозиторий
git clone https://github.com/Daniilsol200/mock-data-generator.git
cd mock-data-generator

# 1. Собрать fat JAR (всё в одном файле)
./gradlew shadowJar
 JAR будет в build/libs/mock-data-generator-1.0.0.jar

# 2. Как запустить тесты
Bash./gradlew test
Ожидаемый результат:
text> Task :test
AppConfigTest > parsesFieldsWithArgsCorrectly() PASSED
CsvFormatterTest > writesHeaderAndRowsCorrectly() PASSED
JsonFormatterTest > formatsCorrectly() PASSED
IntGeneratorTest > generatesWithinRange() PASSED
DataGeneratorTest > generatesCorrectNumberOfRecords() PASSED

BUILD SUCCESSFUL in 3s
5 tests completed, 5 passed

# 3. Как запустить приложение
Через Gradle
Bash./gradlew run
